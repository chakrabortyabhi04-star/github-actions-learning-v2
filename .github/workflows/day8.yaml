name: Learning actions/checkout and marketplace

on:
  push:
    branches:
      - main
    
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4   

      - name: list the files on runner
        run: ls -la  